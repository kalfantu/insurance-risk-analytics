# Minimal dependencies for CI (lint + tests).
# Jupyter and DVC are dev/local-only and are not needed to run the test suite.
pandas==2.2.2
numpy==1.26.4
scipy==1.13.0
matplotlib==3.9.0
seaborn==0.13.2
scikit-learn==1.5.0
flake8==7.0.0
flake8-docstrings==1.7.0
pytest==8.2.0
pytest-cov==5.0.0
